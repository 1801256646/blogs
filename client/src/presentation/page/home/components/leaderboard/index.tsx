import React, { FC } from 'react';
import styles from './index.module.scss'

const Leaderboard: FC = () => {
  console.log(1);

  return (
    <div className={styles.leaderboard}>1</div>
  );
};

export default Leaderboard;
